setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    S3_BUCKET: my-example-bucket
build:
  docker:
    front: front-ng-v1/Dockerfile
    back: back-node-v1/Dockerfile
  config:
    RAILS_ENV: development
release:
  command:
    - ./deployment-tasks.sh
  image: front
# run:
#   web: bundle exec puma -C config/puma.rb
#   worker: python myworker.py
#   asset-syncer:
#     command:
#       - python asset-syncer.py
#     image: worker